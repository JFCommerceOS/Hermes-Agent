# Hermes runtime (HERMES_HOME)
.env
config.yaml
*.log
sessions/
logs/
cron/
node/
/skills/

# Upstream clone is its own git repo
hermes-agent/

# Hybrid setup — track templates; ignore local secrets
hermes-hybrid-setup/.env
hermes-hybrid-setup/logs/*.log
hermes-hybrid-setup/secrets/
